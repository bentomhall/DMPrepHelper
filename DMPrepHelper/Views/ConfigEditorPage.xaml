<Page
    x:Class="DMPrepHelper.Views.ConfigEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DMPrepHelper.ViewModels"
    xmlns:view="using:DMPrepHelper.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <ResourceDictionary>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="4,4,4,8"/>
            </Style>
            <local:ConfigDataTemplateSelector x:Key="ConfigSelector"/>
        </ResourceDictionary>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MaxWidth="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" HorizontalAlignment="Stretch">
            <Button Command="{x:Bind SelectionCommand}" CommandParameter="Cities" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <SymbolIcon Symbol="People"/>
                    <TextBlock Text="Cities" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind SelectionCommand}" CommandParameter="Dungeons" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" >
                    <SymbolIcon Symbol="Map"/>
                    <TextBlock Text="Dungeons" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind SelectionCommand}" CommandParameter="Items" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal"  >
                    <SymbolIcon Symbol="Emoji"/>
                    <TextBlock Text="Items" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind SelectionCommand}" CommandParameter="Nations" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <SymbolIcon Symbol="World"/>
                    <TextBlock Text="Nations" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind SelectionCommand}" CommandParameter="Names" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <SymbolIcon Symbol="AddFriend"/>
                    <TextBlock Text="Names" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind SelectionCommand}" CommandParameter="Personalities" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <SymbolIcon Symbol="Emoji2"/>
                    <TextBlock Text="Personalities" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind SelectionCommand}" CommandParameter="Professions" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <SymbolIcon Symbol="Account"/>
                    <TextBlock Text="Professions" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind SelectionCommand}" CommandParameter="Cultures" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <SymbolIcon Symbol="Contact2"/>
                    <TextBlock Text="Cultures" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind SelectionCommand}" CommandParameter="Regions" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <SymbolIcon Symbol="World"/>
                    <TextBlock Text="Regions" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind SelectionCommand}" CommandParameter="Rumors" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <SymbolIcon Symbol="PostUpdate"/>
                    <TextBlock Text="Rumors" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind SelectionCommand}" CommandParameter="Settlement Types" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal" >
                    <SymbolIcon Symbol="Street"/>
                    <TextBlock Text="Town Sizes" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind SelectionCommand}" CommandParameter="Settlement Roles" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <SymbolIcon Symbol="Filter"/>
                    <TextBlock Text="Town Roles" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
        </StackPanel>
        <ContentControl Grid.Column="1" ContentTemplateSelector="{StaticResource ConfigSelector}" Content="{x:Bind ViewModel.DisplayItem, Mode=OneWay}">
            <ContentControl.Resources>
                <ResourceDictionary Source="ConfigEditorDataTemplates.xaml"/>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</Page>
