<Page
    x:Class="DMPrepHelper.Views.ConfigEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DMPrepHelper.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MaxWidth="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" HorizontalAlignment="Stretch">
            <Button Command="{x:Bind SelectionCommand}" CommandParameter="Cities">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <SymbolIcon Symbol="MapPin"/>
                    <TextBlock Text="Cities" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" >
                    <SymbolIcon Symbol="MapPin"/>
                    <TextBlock Text="Cities" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" >
                    <SymbolIcon Symbol="MapPin"/>
                    <TextBlock Text="Cities" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <SymbolIcon Symbol="MapPin"/>
                    <TextBlock Text="Cities" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
            <Button>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <SymbolIcon Symbol="MapPin"/>
                    <TextBlock Text="Cities" Margin="12,0,0,0"/>
                </StackPanel>
            </Button>
        </StackPanel>
        <TextBox Text="{x:Bind ViewModel.DisplayText, Mode=TwoWay}" Background="Wheat" Grid.Column="1"></TextBox>
    </Grid>
</Page>
